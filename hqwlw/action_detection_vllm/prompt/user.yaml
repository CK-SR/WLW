# user.yaml
role: user
content: |
  Verify the primary person in this single YOLO-cropped image.
  Only detect smoking or takephoto.
  If direct visual evidence is insufficient, return unknown.
  Output ONLY the required minified JSON.